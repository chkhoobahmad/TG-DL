name: ⬇️ Download Multiple Telegram Posts

on:
  workflow_dispatch:
    inputs:
      post_urls:
        description: '🔗 لینک‌های پست تلگرام (با فاصله جدا کنید)'
        required: true
        type: string
        placeholder: 'https://t.me/username/123 https://t.me/username/456'

jobs:
  download-from-telegram:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    permissions:
      contents: write
    steps:
      - name: 🧹 Extreme disk space cleanup
        run: |
          echo "Freeing up massive disk space..."
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          sudo rm -rf /usr/share/swift
          sudo rm -rf /usr/local/.ghcup
          sudo rm -rf /opt/az
          sudo rm -rf /opt/microsoft
          sudo rm -rf /opt/pipx
          sudo rm -rf /usr/lib/jvm
          sudo rm -rf /usr/local/go
          sudo rm -rf /usr/share/gradle
          sudo rm -rf /usr/share/maven
          sudo rm -rf /usr/local/share/chromium
          sudo rm -rf /usr/local/share/gecko_driver
          sudo docker system prune -a -f --volumes 2>/dev/null || true
          sudo apt-get clean
          sudo apt-get autoremove -y
          sudo rm -rf /tmp/*
          sudo rm -rf /var/tmp/*
          rm -rf ~/.cache
          rm -rf ~/.npm
          rm -rf ~/.cargo
          rm -rf ~/.rustup
          
          echo ""
          echo "📊 Available disk space after cleanup:"
          df -h /
          
          AVAILABLE_GB=$(df --output=avail / | tail -n 1 | awk '{print int($1/1024/1024)}')
          if [ $AVAILABLE_GB -lt 5 ]; then
            echo "❌ Only ${AVAILABLE_GB}GB available! Not enough space."
            exit 1
          fi
          echo "✅ ${AVAILABLE_GB}GB available - good to continue!"

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 1

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          npm install telegram
          sudo apt-get update
          sudo apt-get install -y zip curl bc file

      - name: Configure Git for Large Files
        run: |
          git config --global http.postBuffer 524288000
          git config --global http.maxRequestBuffer 100M
          git config --global core.compression 0
          git config --global pack.windowMemory 256m
          git config --global pack.packSizeLimit 50m
          git config --global pack.threads 1
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

      - name: Download Multiple Posts from Telegram
        id: download
        env:
          API_ID: ${{ secrets.TELEGRAM_API_ID }}
          API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
          STRING_SESSION: ${{ secrets.TELEGRAM_STRING_SESSION }}
          POST_URLS: ${{ github.event.inputs.post_urls }}
        run: |
          cat > download_telegram.js << 'EOF'
          const { TelegramClient } = require("telegram");
          const { StringSession } = require("telegram/sessions");
          const fs = require("fs");
          const path = require("path");
          const { execSync } = require("child_process");

          const apiId = parseInt(process.env.API_ID);
          const apiHash = process.env.API_HASH;
          const stringSession = process.env.STRING_SESSION;
          const postUrls = process.env.POST_URLS.split(/\s+/).filter(url => url.trim());

          let lastLoggedPercent = 0;

          function sanitizeFilename(filename) {
            return filename.replace(/[<>:"|?*\\/]/g, '_').replace(/\s+/g, '_');
          }

          async function extractChatAndMsgId(url) {
            const patterns = [
              /https?:\/\/t\.me\/(?:c\/)?([^\/]+)\/(\d+)/,
              /https?:\/\/telegram\.me\/(?:c\/)?([^\/]+)\/(\d+)/,
            ];
            
            let match = null;
            for (const pattern of patterns) {
              match = url.match(pattern);
              if (match) break;
            }
            
            if (!match) {
              throw new Error(`Invalid Telegram post URL format: ${url}`);
            }
            
            let chatId = match[1];
            const msgId = parseInt(match[2]);
            
            if (chatId === 'c' || chatId.startsWith('c/')) {
              chatId = '-100' + chatId.replace('c/', '').replace('c', '');
            } else if (!isNaN(parseInt(chatId)) && chatId.length > 5) {
              chatId = '-100' + chatId;
            }
            
            return { chatId, msgId };
          }

          async function downloadMediaFile(client, media, outputPath, fileName, postId) {
            return new Promise(async (resolve, reject) => {
              try {
                const filePath = await client.downloadMedia(media, {
                  outputFile: outputPath,
                  progressCallback: (downloaded, total) => {
                    const percent = (downloaded / total * 100);
                    if (Math.floor(percent / 10) > Math.floor(lastLoggedPercent / 10)) {
                      lastLoggedPercent = percent;
                      const downloadedMB = downloaded / 1024 / 1024;
                      const totalMB = total / 1024 / 1024;
                      console.log(`      📊 ${percent.toFixed(1)}% (${downloadedMB.toFixed(2)}MB / ${totalMB.toFixed(2)}MB)`);
                    }
                  }
                });
                
                if (filePath && fs.existsSync(filePath)) {
                  const stats = fs.statSync(filePath);
                  if (stats.size > 0) {
                    console.log(`   ✅ Downloaded: ${fileName} (${(stats.size/1024/1024).toFixed(2)}MB)`);
                    resolve(filePath);
                  } else {
                    reject(new Error("File is empty"));
                  }
                } else {
                  reject(new Error("Download failed"));
                }
              } catch (err) {
                reject(err);
              }
            });
          }

          async function getFileNameFromMedia(media) {
            if (media.document && media.document.attributes) {
              for (const attr of media.document.attributes) {
                if (attr.className === 'DocumentAttributeFilename') {
                  return sanitizeFilename(attr.fileName);
                }
              }
            }
            return null;
          }

          async function downloadPost(client, postUrl, index, total) {
            console.log(`\n${'='.repeat(60)}`);
            console.log(`📌 Downloading post ${index + 1}/${total}: ${postUrl}`);
            console.log(`${'='.repeat(60)}`);
            
            const { chatId, msgId } = await extractChatAndMsgId(postUrl);
            
            // Get chat entity
            let chat;
            if (chatId.toString().startsWith('-100')) {
              chat = await client.getEntity(parseInt(chatId));
            } else {
              chat = await client.getEntity(chatId);
            }
            
            // Get channel name
            let channelName = "unknown_channel";
            if (chat.username) {
              channelName = chat.username;
            } else if (chat.title) {
              channelName = sanitizeFilename(chat.title);
            } else {
              channelName = chatId.toString().replace('-100', '');
            }
            
            // Get the target message
            const messages = await client.getMessages(chat, { ids: msgId });
            const message = messages[0];
            
            if (!message) {
              throw new Error(`Message ${msgId} not found in ${channelName}`);
            }
            
            console.log(`✅ Message found!`);
            
            // Create directory structure: /tmp/telegram_downloads/channel_name/post_id/
            const baseTempDir = `/tmp/telegram_downloads`;
            const channelDir = path.join(baseTempDir, channelName);
            const postDir = path.join(channelDir, String(msgId));
            fs.mkdirSync(postDir, { recursive: true });
            
            console.log(`📁 Channel: ${channelName}`);
            console.log(`📁 Post ID: ${msgId}`);
            console.log(`📁 Save directory: ${postDir}`);
            
            const downloadedFiles = [];
            
            // Save message text
            if (message.text && message.text.length > 0) {
              const textFile = path.join(postDir, 'message_text.txt');
              fs.writeFileSync(textFile, message.text);
              downloadedFiles.push(textFile);
              console.log(`📝 Saved message text (${message.text.length} characters)`);
            }
            
            // Download media if exists
            if (message.media) {
              console.log(`📥 Downloading media...`);
              lastLoggedPercent = 0;
              
              let fileName = await getFileNameFromMedia(message.media);
              
              if (!fileName) {
                const mediaType = message.media.className;
                if (mediaType === 'MessageMediaPhoto') {
                  fileName = `photo.jpg`;
                } else if (mediaType === 'MessageMediaDocument') {
                  fileName = `document`;
                } else {
                  fileName = `media`;
                }
              }
              
              let outputPath = path.join(postDir, fileName);
              
              if (!path.extname(outputPath) || path.extname(outputPath) === '') {
                const tempPath = path.join(postDir, `temp_download`);
                try {
                  await downloadMediaFile(client, message.media, tempPath, fileName, msgId);
                  
                  if (fs.existsSync(tempPath)) {
                    const fileType = execSync(`file --mime-type -b "${tempPath}"`).toString().trim();
                    const extMap = {
                      'video/mp4': '.mp4', 'video/x-matroska': '.mkv', 'video/webm': '.webm',
                      'video/quicktime': '.mov', 'video/x-msvideo': '.avi',
                      'audio/mpeg': '.mp3', 'audio/mp4': '.m4a', 'audio/ogg': '.ogg',
                      'image/jpeg': '.jpg', 'image/png': '.png', 'image/gif': '.gif',
                      'application/pdf': '.pdf', 'application/zip': '.zip',
                      'application/x-rar': '.rar', 'application/x-7z-compressed': '.7z'
                    };
                    const ext = extMap[fileType] || '';
                    const finalPath = outputPath + ext;
                    fs.renameSync(tempPath, finalPath);
                    downloadedFiles.push(finalPath);
                    console.log(`   ✅ Saved as: ${path.basename(finalPath)}`);
                  }
                } catch (err) {
                  console.error(`   ❌ Error downloading: ${err.message}`);
                }
              } else {
                try {
                  await downloadMediaFile(client, message.media, outputPath, fileName, msgId);
                  downloadedFiles.push(outputPath);
                } catch (err) {
                  console.error(`   ❌ Error downloading: ${err.message}`);
                }
              }
            } else {
              console.log(`⚠️ No media found in this post`);
            }
            
            // Save post info
            const infoFile = path.join(postDir, 'post_info.json');
            const totalSizeMB = downloadedFiles.reduce((sum, f) => {
              try {
                return sum + fs.statSync(f).size;
              } catch {
                return sum;
              }
            }, 0) / 1024 / 1024;
            
            fs.writeFileSync(infoFile, JSON.stringify({
              url: postUrl,
              channel: channelName,
              channelId: chatId,
              postId: msgId,
              postDate: message.date,
              totalFiles: downloadedFiles.length,
              totalSizeMB: totalSizeMB.toFixed(2),
              hasMedia: !!message.media,
              hasText: !!(message.text && message.text.length > 0)
            }, null, 2));
            
            if (!downloadedFiles.includes(infoFile)) {
              downloadedFiles.push(infoFile);
            }
            
            console.log(`\n📊 Post Summary:`);
            console.log(`   - Total files: ${downloadedFiles.length}`);
            console.log(`   - Total size: ${totalSizeMB.toFixed(2)}MB`);
            
            return {
              channel: channelName,
              postId: msgId,
              postDir: postDir,
              files: downloadedFiles,
              size: totalSizeMB
            };
          }

          async function main() {
            let client = null;
            const allDownloads = [];
            
            try {
              console.log('🚀 Connecting to Telegram...');
              console.log(`📌 Total posts to download: ${postUrls.length}`);
              console.log(`📌 Posts: ${postUrls.join(', ')}\n`);
              
              const session = new StringSession(stringSession);
              client = new TelegramClient(session, apiId, apiHash, {
                connectionRetries: 5,
                useWSS: true,
              });
              
              await client.start({
                phoneNumber: () => Promise.reject(new Error('Use STRING_SESSION')),
                phoneCode: () => Promise.reject(new Error('Use STRING_SESSION')),
                password: () => Promise.reject(new Error('Use STRING_SESSION')),
                onError: (err) => console.error('Connection error:', err),
              });
              
              console.log('✅ Connected successfully!\n');
              
              // Download each post
              for (let i = 0; i < postUrls.length; i++) {
                const result = await downloadPost(client, postUrls[i], i, postUrls.length);
                allDownloads.push(result);
              }
              
              // Save list of all downloaded posts
              const allPostsList = '/tmp/all_posts.txt';
              const allDirs = allDownloads.map(d => d.postDir);
              fs.writeFileSync(allPostsList, allDirs.join('\n'));
              
              // Create master summary
              const masterSummary = {
                totalPosts: allDownloads.length,
                downloadDate: new Date().toISOString(),
                posts: allDownloads.map(d => ({
                  channel: d.channel,
                  postId: d.postId,
                  url: postUrls[allDownloads.indexOf(d)],
                  totalFiles: d.files.length,
                  totalSizeMB: d.size
                }))
              };
              
              const summaryFile = '/tmp/master_summary.json';
              fs.writeFileSync(summaryFile, JSON.stringify(masterSummary, null, 2));
              
              console.log(`\n${'='.repeat(60)}`);
              console.log(`📊 MASTER SUMMARY`);
              console.log(`${'='.repeat(60)}`);
              console.log(`✅ Total posts downloaded: ${allDownloads.length}`);
              console.log(`📁 Total size: ${masterSummary.posts.reduce((sum, p) => sum + p.totalSizeMB, 0).toFixed(2)}MB`);
              console.log(`\n📁 Download directories saved to: /tmp/all_posts.txt`);
              
              // Write output for next steps (save all directories)
              const outputFile = process.env.GITHUB_OUTPUT;
              if (outputFile) {
                fs.appendFileSync(outputFile, `ALL_DIRS=${allDirs.join(',')}\n`);
                fs.appendFileSync(outputFile, `TOTAL_POSTS=${allDownloads.length}\n`);
              }
              
              // Also write to a file as fallback
              fs.writeFileSync('/tmp/all_dirs.txt', allDirs.join('\n'));
              
            } catch (error) {
              console.error('❌ Fatal error:', error);
              process.exit(1);
            } finally {
              if (client) {
                try {
                  console.log('\n👋 Disconnecting from Telegram...');
                  await client.disconnect();
                  console.log('✅ Disconnected successfully');
                } catch (err) {
                  console.log('⚠️ Disconnect warning:', err.message);
                }
              }
            }
          }
          
          // Ignore timeout errors after successful download
          process.on('unhandledRejection', (err) => {
            if (err.message && err.message.includes('TIMEOUT')) {
              console.log('⚠️ Ignoring timeout error (download completed successfully)');
              process.exit(0);
            } else {
              console.error('Unhandled rejection:', err);
              process.exit(1);
            }
          });
          
          main().catch(err => {
            console.error('Main error:', err);
            process.exit(1);
          });
          EOF

          # Run the download script
          node download_telegram.js
          
          # Ensure exit code is 0 even if timeout error occurred but download succeeded
          DOWNLOAD_EXIT=$?
          if [ $DOWNLOAD_EXIT -eq 0 ] || ([ -f "/tmp/all_posts.txt" ] && [ -s "/tmp/all_posts.txt" ]); then
            echo "Download completed successfully (exit code: $DOWNLOAD_EXIT)"
            exit 0
          else
            exit $DOWNLOAD_EXIT
          fi

      - name: Process and Archive All Downloads
        run: |
          # Read all directories from the list
          if [ -f "/tmp/all_dirs.txt" ]; then
            ALL_DIRS=$(cat /tmp/all_dirs.txt)
          else
            echo "❌ No downloads found!"
            exit 1
          fi
          
          BACKUP_DIR="/tmp/backup_$$"
          mkdir -p "$BACKUP_DIR"
          echo "$BACKUP_DIR" > /tmp/backup_dir_path.txt
          
          REPO_OWNER="${{ github.repository_owner }}"
          REPO_NAME="${{ github.event.repository.name }}"
          BRANCH="${GITHUB_REF_NAME}"
          SPLIT_SIZE_MB=45
          
          urlencode() {
            python3 -c "import urllib.parse, sys; print(urllib.parse.quote(sys.argv[1], safe=''))" "$1"
          }
          
          # Process each post directory
          echo "$ALL_DIRS" | while read POST_DIR; do
            if [ -z "$POST_DIR" ] || [ ! -d "$POST_DIR" ]; then
              continue
            fi
            
            CHANNEL_NAME=$(basename $(dirname "$POST_DIR"))
            POST_ID=$(basename "$POST_DIR")
            FOLDER_NAME="${CHANNEL_NAME}_${POST_ID}"
            
            mkdir -p "$BACKUP_DIR/$FOLDER_NAME"
            
            echo ""
            echo "📦 Creating archive for: $CHANNEL_NAME/$POST_ID"
            echo "   Target: $BACKUP_DIR/$FOLDER_NAME"
            
            # Copy all files from post directory
            for FILE in "$POST_DIR"/*; do
              if [ -f "$FILE" ] && [ -s "$FILE" ]; then
                ORIGINAL_NAME=$(basename "$FILE")
                SAFE_NAME=$(echo "$ORIGINAL_NAME" | sed 's/[<>:"|?*]/_/g')
                cp "$FILE" "$BACKUP_DIR/$FOLDER_NAME/$SAFE_NAME"
                SIZE=$(stat -c%s "$FILE" | awk '{printf "%.2fMB", $1/1024/1024}')
                echo "      ✅ Added: $SAFE_NAME ($SIZE)"
              fi
            done
            
            # Create ZIP archive for this post
            cd "$BACKUP_DIR/$FOLDER_NAME"
            
            # Remove any existing zip files
            rm -f *.zip *.z[0-9]* 2>/dev/null || true
            
            # Calculate total size
            TOTAL_SIZE=0
            for FILE in *; do
              if [ -f "$FILE" ]; then
                SIZE=$(stat -c%s "$FILE")
                TOTAL_SIZE=$((TOTAL_SIZE + SIZE))
              fi
            done
            TOTAL_SIZE_MB=$((TOTAL_SIZE / 1024 / 1024))
            
            ARCHIVE_NAME="${POST_ID}"
            
            if [ $TOTAL_SIZE_MB -gt 45 ]; then
              echo "   📀 Creating split archive (${SPLIT_SIZE_MB}MB per part)..."
              zip -r -0 -s ${SPLIT_SIZE_MB}m "${ARCHIVE_NAME}.zip" ./* 2>/dev/null
            else
              echo "   📀 Creating single archive..."
              zip -r -0 "${ARCHIVE_NAME}.zip" ./* 2>/dev/null
            fi
            
            # Remove original files after zipping (keep only the zip)
            for FILE in *; do
              if [ -f "$FILE" ] && [ "$FILE" != "${ARCHIVE_NAME}.zip" ] && [[ ! "$FILE" =~ \.z[0-9]+$ ]]; then
                rm -f "$FILE"
              fi
            done
            
            cd - > /dev/null
            
            # Create README for this post
            FOLDER_ENCODED=$(urlencode "${FOLDER_NAME}")
            DOWNLOAD_LINKS_MD=""
            LINK_NUM=0
            TOTAL_ARCHIVE_SIZE=0
            
            for part_file in $(ls "$BACKUP_DIR/$FOLDER_NAME/"*.zip "$BACKUP_DIR/$FOLDER_NAME/"*.z[0-9]* 2>/dev/null | sort); do
              [ -f "$part_file" ] || continue
              PART_BASENAME=$(basename "$part_file")
              PART_SIZE=$(stat -c%s "$part_file" 2>/dev/null || echo 0)
              PART_SIZE_MB=$(echo "scale=2; $PART_SIZE / 1024 / 1024" | bc)
              TOTAL_ARCHIVE_SIZE=$((TOTAL_ARCHIVE_SIZE + PART_SIZE))
              LINK_NUM=$((LINK_NUM + 1))
            done
            
            TOTAL_ARCHIVE_SIZE_MB=$(echo "scale=2; $TOTAL_ARCHIVE_SIZE / 1024 / 1024" | bc)
            
            # Generate download links
            LINK_NUM=0
            for part_file in $(ls "$BACKUP_DIR/$FOLDER_NAME/"*.zip "$BACKUP_DIR/$FOLDER_NAME/"*.z[0-9]* 2>/dev/null | sort); do
              [ -f "$part_file" ] || continue
              PART_BASENAME=$(basename "$part_file")
              PART_ENCODED=$(urlencode "${PART_BASENAME}")
              RAW_LINK="https://github.com/${REPO_OWNER}/${REPO_NAME}/raw/${BRANCH}/downloads/${FOLDER_ENCODED}/${PART_ENCODED}"
              LINK_NUM=$((LINK_NUM + 1))
              DOWNLOAD_LINKS_MD="${DOWNLOAD_LINKS_MD}| ${LINK_NUM} | \`${PART_BASENAME}\` | [Download](${RAW_LINK}) |"$'\n'
            done
            
            # Read post info if exists
            POST_INFO_FILE="$BACKUP_DIR/$FOLDER_NAME/post_info.json"
            if [ ! -f "$POST_INFO_FILE" ]; then
              # Try to find it in the original directory
              ORIGINAL_INFO="$POST_DIR/post_info.json"
              if [ -f "$ORIGINAL_INFO" ]; then
                cp "$ORIGINAL_INFO" "$BACKUP_DIR/$FOLDER_NAME/"
              fi
            fi
            
            {
              echo "# 📥 Telegram Post ${POST_ID}"
              echo ""
              echo "---"
              echo ""
              echo "## 📊 Download Information"
              echo ""
              echo "| Property | Value |"
              echo "|----------|-------|"
              echo "| **Channel** | \`${CHANNEL_NAME}\` |"
              echo "| **Post ID** | \`${POST_ID}\` |"
              echo "| **Archive Size** | ${TOTAL_ARCHIVE_SIZE_MB} MB (${LINK_NUM} parts) |"
              echo "| **Status** | ✅ **Complete** |"
              echo ""
              echo "---"
              echo ""
              echo "## 🔗 Download Links"
              echo ""
              echo "| # | File | Link |"
              echo "|---|------|------|"
              printf '%s' "${DOWNLOAD_LINKS_MD}"
              echo ""
              echo "---"
              echo ""
              echo "## 📖 How to Extract"
              echo ""
              echo "1. **Download all parts** (\`.zip\`, \`.z01\`, \`.z02\`...)"
              echo "2. **Open** \`${POST_ID}.zip\` with WinRAR or 7-Zip"
              echo "3. **Extract** — all parts combine automatically"
              echo ""
              echo "---"
              echo ""
              echo "*🛠️ Created by [avasam.ir](https://avasam.ir)*"
            } > "$BACKUP_DIR/$FOLDER_NAME/README.md"
            
            echo "   ✅ Archive created for post ${POST_ID} (${LINK_NUM} parts, ${TOTAL_ARCHIVE_SIZE_MB}MB)"
          done
          
          echo ""
          echo "✅ All posts archived successfully!"

      - name: Update main README.md
        run: |
          BACKUP_DIR=$(cat /tmp/backup_dir_path.txt)
          REPO_OWNER="${{ github.repository_owner }}"
          REPO_NAME="${{ github.event.repository.name }}"
          BRANCH="${GITHUB_REF_NAME}"
          REPO_URL="${{ github.server_url }}/${{ github.repository }}/tree/${{ github.ref_name }}/downloads"

          mkdir -p downloads
          if [ -d "$BACKUP_DIR" ] && [ "$(ls -A $BACKUP_DIR 2>/dev/null)" ]; then
            cp -r "$BACKUP_DIR"/* downloads/
          fi

          FOLDERS=$(find downloads -mindepth 1 -maxdepth 1 -type d -printf '%f\n' 2>/dev/null | sort)

          if [ -z "$FOLDERS" ]; then
            echo "No folders found in downloads"
            exit 0
          fi

          urlencode() {
            python3 -c "import urllib.parse, sys; print(urllib.parse.quote(sys.argv[1], safe=''))" "$1"
          }

          {
            echo "# 📥 Downloads from Telegram"
            echo ""
            echo "---"
            echo ""
            echo "## 📁 Available Downloads"
            echo ""
            for FOLDER in $FOLDERS; do
              if [ -n "$FOLDER" ]; then
                FOLDER_ENCODED=$(urlencode "${FOLDER}")
                echo "- [${FOLDER}](./${FOLDER_ENCODED})"
              fi
            done
            echo ""
            echo "---"
            echo ""
            echo "*🛠️ Created by [avasam.ir](https://avasam.ir)*"
          } > "downloads/README.md"

      - name: Commit & Push
        run: |
          BRANCH="${GITHUB_REF_NAME}"
          BACKUP_DIR=$(cat /tmp/backup_dir_path.txt)

          BACKUP_FILE_COUNT=$(find "$BACKUP_DIR" -type f 2>/dev/null | wc -l)
          echo "Total files to push: $BACKUP_FILE_COUNT"

          if [ "$BACKUP_FILE_COUNT" -eq 0 ]; then
            echo "Nothing to push"
            exit 0
          fi

          git fetch origin "$BRANCH" 2>/dev/null || true
          git reset --hard origin/"$BRANCH" 2>/dev/null || true

          mkdir -p downloads
          cp -r "$BACKUP_DIR"/* downloads/ 2>/dev/null || true

          git add -f downloads/
          git add README.md

          if ! git diff --cached --quiet; then
            git commit -m "Download multiple Telegram posts [skip ci]"
            
            for i in {1..5}; do
              echo "Push attempt $i/5..."
              if timeout 300 git push origin HEAD:"$BRANCH" 2>&1; then
                echo "✅ Push successful!"
                break
              else
                echo "Push failed, retry $i/5..."
                if [ $i -lt 5 ]; then
                  sleep 10
                  git fetch origin "$BRANCH" 2>/dev/null || true
                  git reset --hard origin/"$BRANCH" 2>/dev/null || true
                  cp -r "$BACKUP_DIR"/* downloads/ 2>/dev/null || true
                  git add -f downloads/
                  git add README.md
                  if ! git diff --cached --quiet; then
                    git commit -m "Download multiple Telegram posts [skip ci]"
                  fi
                fi
              fi
            done
          fi

          echo "✅ Done!"
